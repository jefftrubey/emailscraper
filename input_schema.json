{
  "title": "CSV → CSV Email Extractor (URL or KV store)",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "csvUrl": {
      "title": "CSV URL (https://...)",
      "type": "string",
      "editor": "text",
      "description": "Public URL to the CSV. Leave blank if using Key-Value Store."
    },
    "useKvInput": {
      "title": "Use Key-Value Store 'INPUT.csv'",
      "type": "boolean",
      "editor": "checkbox",
      "default": false,
      "description": "Enable if you upload a CSV named INPUT.csv to the actor run's Key-Value Store."
    },
    "urlCol": {
      "title": "URL Column Name",
      "type": "string",
      "editor": "text",
      "default": "Staff Page URL",
      "description": "The exact column name in your CSV that contains URLs to crawl."
    },
    "waitMs": {
      "title": "Page Load Timeout (ms)",
      "type": "integer",
      "editor": "number",
      "default": 45000
    },
    "headful": {
      "title": "Run in visible browser mode (debug)",
      "type": "boolean",
      "editor": "checkbox",
      "default": false
    },
    "limit": {
      "title": "Max Rows to Process (0 = all)",
      "type": "integer",
      "editor": "number",
      "default": 0
    },
    "force": {
      "title": "Overwrite existing 'Found Emails' values",
      "type": "boolean",
      "editor": "checkbox",
      "default": false
    }
  },
  "required": []
}
